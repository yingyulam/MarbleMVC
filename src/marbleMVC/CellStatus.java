package marbleMVC;

/**
 * This is the enum type that represents the status of cell. Cells on the game board could be
 * forbidden to play on, occupied by a marble, or empty.
 */
public enum CellStatus {
  FORBIDDEN, OCCUPIED, EMPTY
}
